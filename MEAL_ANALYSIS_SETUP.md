# 🍽️ הגדרת תכונת ניתוח ארוחות - Numa Health App

## 📋 סקירה כללית

תכונת ניתוח הארוחות מאפשרת למשתמשים לצלם תמונות של הארוחות שלהם ולקבל:
- 🥇 זיהוי מרכיבים וכמויות
- 🥈 חישוב ערכים תזונתיים
- 🥉 ציון התאמה למטרה האישית
- 🏅 טיפים מותאמים אישית

## 🔧 הגדרה נדרשת

### 1. קבלת מפתח OpenAI API

1. היכנס ל-[OpenAI Platform](https://platform.openai.com/)
2. צור חשבון או התחבר לחשבון קיים
3. עבור ל-[API Keys](https://platform.openai.com/api-keys)
4. לחץ על "Create new secret key"
5. העתק את המפתח (שמור אותו במקום בטוח!)

### 2. הגדרת המפתח באפליקציה

1. פתח את הקובץ `.env` בשורש הפרויקט
2. החלף את `YOUR_OPENAI_API_KEY_HERE` במפתח שלך:

```bash
OPENAI_API_KEY=sk-your-actual-api-key-here
```

⚠️ **חשוב**: 
- ודא שהמפתח מתחיל ב-`sk-`
- אל תשתף את הקובץ `.env` עם אף אחד
- הקובץ כבר נוסף ל-`.gitignore` לבטיחות

### 3. עלויות OpenAI

- **GPT-4 Vision**: ~$0.01-0.03 לתמונה
- **GPT-4 Text**: ~$0.03-0.06 לניתוח
- **סה"כ לארוחה**: ~$0.05-0.10

💡 **טיפ**: התחל עם credit חינמי של $5 מ-OpenAI

## 🚀 איך זה עובד

### תהליך הניתוח (4 שלבים):

1. **🥇 זיהוי מרכיבים**
   - OpenAI Vision מנתח את התמונה
   - מזהה מרכיבים וכמויות מוערכות
   - מחזיר רמת ביטחון לכל מרכיב

2. **🥈 חישוב תזונתי**
   - מחשב קלוריות, חלבון, פחמימות, שומן
   - מבוסס על מאגר נתונים תזונתיים
   - מתאים למזון ישראלי ובינלאומי

3. **🥉 ציון התאמה**
   - משווה למטרת המשתמש (הפחתה/שמירה/עלייה במשקל)
   - לוקח בחשבון גיל, מין, משקל, גובה
   - נותן ציון 1-100 עם נימוק מפורט

4. **🏅 טיפ מותאם**
   - המלצה אישית לשיפור
   - מבוסס על הניתוח והמטרה
   - טיפים מעשיים וקלים ליישום

## 🛠️ פתרון בעיות

### שגיאות נפוצות:

**"נדרש להגדיר מפתח OpenAI API בקובץ .env"**
- ודא שיש לך קובץ `.env` בשורש הפרויקט
- ודא שהמפתח מוגדר: `OPENAI_API_KEY=sk-your-key`
- ודא שהמפתח מתחיל ב-`sk-`
- אתחל את האפליקציה לאחר שינוי ה-`.env`

**"שגיאה בחיבור ל-OpenAI"**
- בדוק חיבור לאינטרנט
- ודא שהמפתח תקף ופעיל
- בדוק שיש לך credit ב-OpenAI account

**"לא ניתן לנתח את התמונה"**
- ודא שהתמונה ברורה ומכילה אוכל
- נסה תמונה אחרת
- בדוק שהתמונה לא גדולה מדי

## 📱 שימוש באפליקציה

1. לחץ על "Start Tracking" בעמוד הבית
2. צלם תמונה של הארוחה או בחר מהגלריה
3. לחץ "Save Meal"
4. המתן לניתוח (10-30 שניות)
5. צפה בתוצאות הניתוח המפורטות

## 🔒 אבטחה ופרטיות

- מפתח ה-API נשמר בקובץ `.env` המוגן
- הקובץ `.env` לא נשמר ב-Git (נוסף ל-`.gitignore`)
- התמונות נשמרות ב-Firebase Storage המוצפן
- הניתוחים נשמרים ב-Firestore
- לא נשמרים נתונים אישיים ב-OpenAI

## 🎯 תכונות עתידיות

- [ ] ניתוח מגמות תזונתיות
- [ ] השוואה עם יעדים יומיים
- [ ] המלצות ארוחות
- [ ] שיתוף עם מאמן תזונה
- [ ] תמיכה בשפות נוספות

## 📞 תמיכה

אם נתקלת בבעיות:
1. בדוק את הקונסול לשגיאות
2. ודא שכל ההגדרות נכונות
3. אתחל את האפליקציה לאחר שינוי `.env`
4. נסה לנקות cache: `expo r -c`

---

**🎉 מוכן לשימוש!** לאחר הגדרת המפתח בקובץ `.env`, תוכל להתחיל לנתח ארוחות ולקבל תובנות תזונתיות מותאמות אישית. 